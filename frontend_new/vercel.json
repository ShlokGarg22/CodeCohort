{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://code-cohort-aoac.vercel.app/api/$1"
    }
  ],
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ]
}
